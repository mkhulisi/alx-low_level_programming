#include "main.h"
/**
* mul - get product of two numbers
* @a: first parameter number
* @b: second parameter number
* Return: the product of first and second parameter
*/
int mul(int a, int b)
{
	return (a * b);
}
